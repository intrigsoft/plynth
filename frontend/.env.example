# Frontend (Vite) — DioscHub assistant embed. Copy to frontend/.env and fill in.
# These are public (shipped in the bundle): the embed key is a public, per-assistant key.
VITE_DIOSC_HUB_URL=http://localhost:3333
VITE_DIOSC_EMBED_KEY=
VITE_DIOSC_ASSISTANT_ID=
